# OpEx Cycle Time Studies for Windows

This Windows desktop wrapper opens the hosted OpEx Cycle Time Studies app.

Each user selects OpenAI or Anthropic and enters their own API key in AI Settings. The key is stored in that user's local app profile and is not bundled into the installer.
