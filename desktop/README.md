# OpEx Cycle Time Studies for Windows

This Windows desktop wrapper opens the hosted OpEx Cycle Time Studies app.

Each user enters their own OpenAI API key in AI Settings. The key is stored in that user's local app profile and is not bundled into the installer.
